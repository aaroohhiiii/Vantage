import type { Config } from "tailwindcss"

const config: Config = {
  theme: {
    extend: {
      fontFamily: {
        sans: ['"PP Mori"', "system-ui", "-apple-system", "sans-serif"],
      },
    },
  },
}

export default config
